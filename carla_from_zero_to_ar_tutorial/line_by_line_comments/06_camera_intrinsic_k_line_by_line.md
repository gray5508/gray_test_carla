# 06_camera_intrinsic_k.py 逐行注释

说明：本文件不替代源码运行；它是给初学者阅读的逐行讲义。

| 行号 | 代码 | 解释 |
|---:|---|---|
| 1 | `"""` | 文档字符串边界，开始或结束本文件/函数/类的说明文字。 |
| 2 | `06_camera_intrinsic_k.py` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 3 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 4 | `本节目标：` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 5 | `  1. 理解相机内参 K；` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 6 | `  2. 理解 fx/fy/cx/cy；` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 7 | `  3. 用纯数学例子把 camera coordinate 投影到 pixel。` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 8 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 9 | `这一节不连接 CARLA server。` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 10 | `它只讲相机几何里最基础的 pinhole model。` | 文档字符串内容，用自然语言解释当前文件、函数或类的目标和使用方式。 |
| 11 | `"""` | 文档字符串边界，开始或结束本文件/函数/类的说明文字。 |
| 12 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 13 | `import numpy as np` | 导入 `numpy as np` 模块，供后续代码使用其中的函数、类或常量。 |
| 14 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 15 | `from common import CAMERA_FOV` | 从 `common` 导入 `CAMERA_FOV`，避免后面反复写模块前缀。 |
| 16 | `from common import WINDOW_HEIGHT` | 从 `common` 导入 `WINDOW_HEIGHT`，避免后面反复写模块前缀。 |
| 17 | `from common import WINDOW_WIDTH` | 从 `common` 导入 `WINDOW_WIDTH`，避免后面反复写模块前缀。 |
| 18 | `from common import build_camera_intrinsic_k` | 从 `common` 导入 `build_camera_intrinsic_k`，避免后面反复写模块前缀。 |
| 19 | `from common import camera_cv_to_pixel` | 从 `common` 导入 `camera_cv_to_pixel`，避免后面反复写模块前缀。 |
| 20 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 21 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 22 | `def main():` | 定义 `main` 函数，把一段可复用逻辑封装起来。 |
| 23 | `    k = build_camera_intrinsic_k(WINDOW_WIDTH, WINDOW_HEIGHT, CAMERA_FOV)` | 构造相机内参矩阵 K，用于 camera 坐标和像素坐标之间的转换。 |
| 24 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 25 | `    print("Image size:")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 26 | `    print("  width  =", WINDOW_WIDTH)` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 27 | `    print("  height =", WINDOW_HEIGHT)` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 28 | `    print("FOV:")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 29 | `    print("  {} degrees".format(CAMERA_FOV))` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 30 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 31 | `    print("\\nIntrinsic matrix K:")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 32 | `    print(k)` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 33 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 34 | `    print("\\nMeaning:")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 35 | `    print("  fx = {:.3f}".format(k[0, 0]))` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 36 | `    print("  fy = {:.3f}".format(k[1, 1]))` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 37 | `    print("  cx = {:.3f}".format(k[0, 2]))` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 38 | `    print("  cy = {:.3f}".format(k[1, 2]))` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 39 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 40 | `    print("\\nOpenCV camera coordinate convention:")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 41 | `    print("  x: right")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 42 | `    print("  y: down")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 43 | `    print("  z: forward")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 44 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 45 | `    sample_points = [` | 给 `sample_points` 赋值；右侧表达式计算或创建结果，左侧变量保存下来供后续使用。 |
| 46 | `        ("center 10m", np.array([0.0, 0.0, 10.0])),` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 47 | `        ("right 1m at 10m", np.array([1.0, 0.0, 10.0])),` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 48 | `        ("left 1m at 10m", np.array([-1.0, 0.0, 10.0])),` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 49 | `        ("down 1m at 10m", np.array([0.0, 1.0, 10.0])),` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 50 | `        ("up 1m at 10m", np.array([0.0, -1.0, 10.0])),` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 51 | `        ("center 20m", np.array([0.0, 0.0, 20.0])),` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 52 | `    ]` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
| 53 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 54 | `    print("\\nCamera coordinate -> pixel:")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 55 | `    for name, point_cv in sample_points:` | 循环语句：依次处理一个序列里的每个元素。 |
| 56 | `        pixel = camera_cv_to_pixel(point_cv, k)` | 给 `pixel` 赋值；右侧表达式计算或创建结果，左侧变量保存下来供后续使用。 |
| 57 | `        print("  {:16s} camera {} -> pixel {}".format(name, point_cv, pixel))` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 58 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 59 | `    print("\\n观察：")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 60 | `    print("  z 越大，同样 1 米横向偏移对应的像素偏移越小。")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 61 | `    print("  这就是远处检测点更容易因为几个像素误差导致世界坐标误差放大的原因。")` | 向终端打印信息，帮助你观察程序当前状态或调试变量。 |
| 62 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 63 | ` ` | 空行，用来把代码分成更容易阅读的逻辑段落。 |
| 64 | `if __name__ == "__main__":` | Python 脚本入口判断：只有直接运行本文件时，下面的 `main()` 才会执行。 |
| 65 | `    main()` | 函数调用、对象构造或容器内容的一部分；结合上下文完成当前代码块的参数配置。 |
